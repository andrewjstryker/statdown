# Package .onLoad Function

This function is executed when the package is loaded. It registers the
custom S3 method for htmlwidgets.

## Usage

``` r
.onLoad(libname, pkgname)
```

## Arguments

- libname:

  The library name.

- pkgname:

  The package name.
