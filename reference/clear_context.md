# Clear the render context

Resets the stored DependencyManager to `NULL`. Called via
[`on.exit()`](https://rdrr.io/r/base/on.exit.html) in
[`statdown_render()`](https://andrewjstryker.github.io/statdown/reference/statdown_render.md).

## Usage

``` r
clear_context()
```
