% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{statdown_render}
\alias{statdown_render}
\title{Render an R Markdown file to CommonMark}
\usage{
statdown_render(
  input,
  output_root = NULL,
  url_root = NULL,
  cdn = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{input}{A character string specifying the input file (must have \code{.Rmd}
or \code{.Rmarkdown} extension).}

\item{output_root}{Character. Filesystem directory where depkit copies widget
assets. Defaults to a \verb{libs/} directory next to the input file.}

\item{url_root}{Character. URL prefix used in emitted \verb{<link>} / \verb{<script>}
tags. Defaults to \code{"libs"}.}

\item{cdn}{Logical. If \code{TRUE} (the default), depkit emits CDN URLs with
SRI integrity for JS assets, falling back to local copies when the CDN
is unreachable. Set to \code{FALSE} to serve all assets locally.}

\item{quiet}{A logical value indicating whether to suppress knitr output
messages (default: \code{FALSE}).}
}
\value{
The path to the rendered \code{.md} file (invisibly).
}
\description{
Wraps \code{\link[knitr:knit]{knitr::knit()}} to render \code{.Rmd} or \code{.Rmarkdown} files into
\href{https://spec.commonmark.org/current/}{CommonMark} \code{.md}. When htmlwidgets
are present, their HTML is inlined and CSS/JS dependency tags are injected
via depkit.
}
\details{
The output \code{.md} is compatible with any static site generator that accepts
CommonMark (Hugo, Jekyll, Eleventy, etc.).

The output filename is derived by replacing the input extension with \code{.md}.
}
